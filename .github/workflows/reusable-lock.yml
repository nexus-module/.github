name: "Lock Threads"

on:
  workflow_call:
    inputs:
      issue-inactive-days:
        description: "Number of days before an issue is considered inactive"
        required: false
        type: string
        default: "30"
      pr-inactive-days:
        description: "Number of days before a PR is considered inactive"
        required: false
        type: string
        default: "30"

jobs:
  lock:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: dessant/lock-threads@89ae32b08ed1a541efecbab17912962a5e38981c # v6.0.2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          issue-comment: >
            I'm going to lock this issue because it has been closed for _30 days_ ⏳. This helps our maintainers find and focus on the active issues.
            If you have found a problem that seems similar to this, please open a new issue and complete the issue template so we can capture all the details necessary to investigate further.
          issue-inactive-days: ${{ inputs.issue-inactive-days }}
          pr-comment: >
            I'm going to lock this pull request because it has been closed for _30 days_ ⏳. This helps our maintainers find and focus on the active issues.
            If you have found a problem that seems related to this change, please open a new issue and complete the issue template so we can capture all the details necessary to investigate further.
          pr-inactive-days: ${{ inputs.pr-inactive-days }}
